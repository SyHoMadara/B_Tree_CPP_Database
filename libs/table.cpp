#include "table.h"
#include "BTree.h"
#include "vector"

template<typename T>
void table<T>::create(string &name, const table::COLUMN_TYPE& columns) {

}

template<typename T>
void table<T>::insert(const vector<string>& fields) {

}

template<typename T>
table<T>::table(string basicString, vector<NODE_HASH_TYPE> vector) {

}
